# Заготовка: сервис grafana добавляется в compose в фазе 6 (наблюдаемость).
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
