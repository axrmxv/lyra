# Установка: pre-commit install && pre-commit install --hook-type commit-msg
# Обход хуков (--no-verify) запрещён — .claude/rules/git.md
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.6.9
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format

  - repo: https://github.com/pre-commit/mirrors-prettier
    rev: v3.1.0
    hooks:
      - id: prettier
        # mirrors-prettier заморожен на prettier 3.1.0; версия хука обязана
        # совпадать с node_modules/CI (lockfile), иначе хук переписывает
        # файлы в старый стиль и коммит «зависает» в цикле с ci/lint
        additional_dependencies: ["prettier@3.9.5"]
        files: ^frontend/.*\.(ts|tsx|css|json)$
        exclude: package-lock\.json

  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.18.4
    hooks:
      - id: gitleaks

  - repo: https://github.com/alessandrojcm/commitlint-pre-commit-hook
    rev: v9.16.0
    hooks:
      - id: commitlint
        stages: [commit-msg]
        additional_dependencies: ["@commitlint/config-conventional"]
